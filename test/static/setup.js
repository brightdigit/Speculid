require(['chai', 'mocha'], function (chai) {

  // Chai
  var should = chai.should();
  //  chai.use(chaiJquery);
  /*globals mocha */
  mocha.setup('bdd');

  //require([
  //  'models/Account',
  //], function(require) {
  //mocha.run();
  return mocha;
  //});
});